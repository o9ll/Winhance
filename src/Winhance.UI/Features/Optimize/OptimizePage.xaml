<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winhance.UI.Features.Optimize.OptimizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winhance.UI.Features.Optimize"
    xmlns:models="using:Winhance.UI.Features.Common.Models"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:fluentIcons="using:FluentIcons.WinUI"
    xmlns:helpers="using:Winhance.UI.Features.Common.Helpers"
    x:DefaultBindMode="OneWay"
    NavigationCacheMode="Enabled">

    <Grid Padding="0,0,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- NavRow -->
            <RowDefinition Height="*"/>     <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Row 0: Page Header with Icon (title left, search right) -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16">
                <Viewbox Width="64" Height="64" Stretch="Uniform">
                    <fluentIcons:FluentIcon Icon="Gauge" IconVariant="Color" />
                </Viewbox>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock
                        Text="{x:Bind ViewModel.PageTitle}"
                        Style="{StaticResource TitleTextBlockStyle}"
                        FontWeight="Bold"
                        FontSize="30"
                        AutomationProperties.HeadingLevel="Level1"/>
                    <TextBlock
                        Text="{x:Bind ViewModel.PageDescription}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
            <Grid Grid.Column="1" Width="220" VerticalAlignment="Bottom">
                <AutoSuggestBox
                    x:Name="SearchBox"
                    PlaceholderText="{x:Bind ViewModel.SearchPlaceholder}"
                    helpers:AutoSuggestBoxExtensions.PlaceholderEllipsis="True"
                    QueryIcon="Find"
                    Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    ItemsSource="{x:Bind ViewModel.SearchSuggestions}"
                    SuggestionChosen="SearchBox_SuggestionChosen"
                    QuerySubmitted="SearchBox_QuerySubmitted">
                    <AutoSuggestBox.ItemTemplate>
                        <DataTemplate x:DataType="models:SearchSuggestionItem">
                            <Grid Padding="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{x:Bind SettingName, Mode=OneTime}" Style="{StaticResource BodyTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind SectionDisplayName, Mode=OneTime}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </AutoSuggestBox.ItemTemplate>
                </AutoSuggestBox>
            </Grid>
        </Grid>

        <!-- Row 1: Navigation Bar -->
        <Grid Grid.Row="1" Margin="0,12,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>     <!-- Breadcrumb -->
                <ColumnDefinition Width="Auto"/>  <!-- Quick Actions -->
                <ColumnDefinition Width="Auto"/>  <!-- View -->
            </Grid.ColumnDefinitions>

            <!-- Breadcrumb navigation -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                <!-- "Optimize" root button - navigates to overview -->
                <Button
                    x:Name="BreadcrumbRoot"
                    Click="BreadcrumbOverview_Click"
                    AutomationProperties.Name="{x:Bind ViewModel.BreadcrumbRootText}"
                    Padding="8,4">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Viewbox Width="16" Height="16" VerticalAlignment="Center" Margin="0,-2,0,0">
                            <fluentIcons:FluentIcon Icon="Gauge" IconVariant="Color" />
                        </Viewbox>
                        <TextBlock Text="{x:Bind ViewModel.BreadcrumbRootText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Separator (visible only on detail pages) -->
                <fluentIcons:FluentIcon
                    x:Name="BreadcrumbSeparator"
                    Icon="ChevronRight"
                    IconVariant="Regular"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    VerticalAlignment="Center"
                    Visibility="Collapsed"/>

                <!-- Current section dropdown with icon (visible only on detail pages) -->
                <DropDownButton
                    x:Name="BreadcrumbSection"
                    Visibility="Collapsed"
                    Padding="8,4">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <!-- PathIcon for SVG-based icons -->
                            <Viewbox x:Name="BreadcrumbSectionIconBox" Width="16" Height="16" VerticalAlignment="Center" Margin="0,-2,0,0">
                                <PathIcon x:Name="BreadcrumbSectionIcon" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </Viewbox>
                            <!-- SymbolIcon for FluentIcons-based icons (Update, Sound) -->
                            <fluentIcons:FluentIcon x:Name="BreadcrumbSectionSymbol" IconVariant="Regular" FontSize="16" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Visibility="Collapsed"/>
                            <TextBlock x:Name="BreadcrumbSectionText" VerticalAlignment="Center"/>
                            <InfoBadge x:Name="BreadcrumbSectionBadge" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <Flyout x:Name="BreadcrumbFlyout" Placement="BottomEdgeAlignedLeft">
                            <Flyout.FlyoutPresenterStyle>
                                <Style TargetType="FlyoutPresenter">
                                    <Setter Property="Padding" Value="4"/>
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Flyout.FlyoutPresenterStyle>
                            <StackPanel Spacing="0" MinWidth="180">
                                <Button x:Name="FlyoutButtonPrivacy" Click="NavigatePrivacy_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Viewbox Grid.Column="0" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <PathIcon Data="{StaticResource PrivacyIconPath}"/>
                                        </Viewbox>
                                        <TextBlock x:Name="FlyoutTextPrivacy" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgePrivacy" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="FlyoutButtonPower" Click="NavigatePower_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Viewbox Grid.Column="0" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <PathIcon Data="{StaticResource PowerIconPath}"/>
                                        </Viewbox>
                                        <TextBlock x:Name="FlyoutTextPower" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgePower" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="FlyoutButtonGaming" Click="NavigateGaming_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Viewbox Grid.Column="0" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <PathIcon Data="{StaticResource GamingIconPath}"/>
                                        </Viewbox>
                                        <TextBlock x:Name="FlyoutTextGaming" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgeGaming" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="FlyoutButtonUpdate" Click="NavigateUpdate_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <fluentIcons:FluentIcon Grid.Column="0" Icon="ArrowSync" IconVariant="Regular" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="FlyoutTextUpdate" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgeUpdate" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="FlyoutButtonNotification" Click="NavigateNotification_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Viewbox Grid.Column="0" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <PathIcon Data="{StaticResource NotificationIconPath}"/>
                                        </Viewbox>
                                        <TextBlock x:Name="FlyoutTextNotification" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgeNotification" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="FlyoutButtonSound" Click="NavigateSound_Click" HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent" BorderThickness="0" Padding="8,5">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <fluentIcons:FluentIcon Grid.Column="0" Icon="Speaker2" IconVariant="Regular" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="FlyoutTextSound" Grid.Column="1" VerticalAlignment="Center"/>
                                        <InfoBadge x:Name="FlyoutBadgeSound" Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </Flyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>

            <!-- Quick Actions dropdown -->
            <DropDownButton x:Name="QuickActionsButton" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,4,0">
                <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE945;" FontSize="14"/>
                        <TextBlock x:Name="QuickActionsLabel" VerticalAlignment="Center"/>
                    </StackPanel>
                </DropDownButton.Content>
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                        <MenuFlyoutItem x:Name="ApplyRecommendedItem" Click="ApplyRecommended_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon x:Name="ApplyRecommendedIcon" Glyph="&#xE735;" FontSize="14"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem x:Name="ResetDefaultsItem" Click="ResetDefaults_Click">
                            <MenuFlyoutItem.Icon>
                                <PathIcon x:Name="ResetDefaultsIcon" Data="{StaticResource WindowsLogoIconPath}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <!-- View dropdown -->
            <DropDownButton x:Name="ViewMenuButton" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,0,0">
                <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE7B3;" FontSize="14"/>
                        <TextBlock x:Name="ViewMenuLabel" VerticalAlignment="Center"/>
                    </StackPanel>
                </DropDownButton.Content>
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                        <ToggleMenuFlyoutItem x:Name="TechnicalDetailsToggleItem" Click="ViewTechnicalDetails_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE946;"/>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <ToggleMenuFlyoutItem x:Name="InfoBadgesToggleItem" Click="ViewInfoBadges_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <PathIcon Data="{StaticResource BookInformationVariantIconPath}"/>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <ToggleMenuFlyoutItem x:Name="NewBadgesToggleItem" Click="ViewNewBadges_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <PathIcon Data="{StaticResource NewBoxIconPath}"/>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <MenuFlyoutSeparator x:Name="ShowOnlyChangesSeparator" Visibility="Collapsed"/>
                        <ToggleMenuFlyoutItem x:Name="ShowOnlyChangesToggleItem" Click="ViewShowOnlyChanges_Click" Visibility="Collapsed">
                            <ToggleMenuFlyoutItem.Icon>
                                <PathIcon Data="{StaticResource CogTransferIconPath}"/>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>

        </Grid>

        <!-- Row 2: Content Container with rounded corners and lighter background -->
        <Border
            Grid.Row="2"
            Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}"
            CornerRadius="8"
            Margin="0,0,0,12">
            <Grid>
                <!-- Overview Content (visible when not on detail page) -->
                <Grid x:Name="OverviewContent">
                    <!-- Loading State -->
                    <ProgressRing
                        IsActive="{x:Bind ViewModel.IsLoading}"
                        Visibility="{x:Bind ViewModel.IsLoading}"
                        Width="48"
                        Height="48"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>

                    <!-- Overview Cards -->
                    <ScrollView x:Name="OverviewScrollView" Visibility="{x:Bind ViewModel.IsNotLoading}">
                        <!-- Wrap StackPanel in Grid to avoid visual glitches with MaxWidth -->
                        <Grid>
                        <StackPanel Spacing="8" MaxWidth="{StaticResource SettingsContentMaxWidth}" Margin="{StaticResource SettingsContentMargin}" HorizontalAlignment="Stretch" Padding="0,12,0,12">

                            <!-- Privacy & Security Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.PrivacyViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.PrivacyViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="PrivacyCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.PrivacyViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="PrivacyNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="PrivacyNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <PathIcon Data="{StaticResource PrivacyIconPath}"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="PrivacyBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="PrivacyOverviewBadges" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="PrivacyRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="PrivacyRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="PrivacyDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="PrivacyDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="PrivacyCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="PrivacyCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                            <!-- Power Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.PowerViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.PowerViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="PowerCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.PowerViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="PowerNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="PowerNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <PathIcon Data="{StaticResource PowerIconPath}"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="PowerBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="PowerOverviewBadges" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="PowerRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="PowerRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="PowerDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="PowerDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="PowerCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="PowerCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                            <!-- Gaming and Performance Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.GamingViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.GamingViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="GamingCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.GamingViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="GamingNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="GamingNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <PathIcon Data="{StaticResource GamingIconPath}"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="GamingBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="GamingOverviewBadges" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="GamingRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="GamingRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="GamingDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="GamingDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="GamingCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="GamingCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                            <!-- Update Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.UpdateViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.UpdateViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="UpdateCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.UpdateViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="UpdateNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="UpdateNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <fluentIcons:FluentIcon Icon="ArrowSync" IconVariant="Regular"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="UpdateBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="UpdateOverviewPills" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="UpdateRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="UpdateRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="UpdateDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="UpdateDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="UpdateCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="UpdateCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                            <!-- Notifications Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.NotificationViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.NotificationViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="NotificationCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.NotificationViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="NotificationNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="NotificationNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <PathIcon Data="{StaticResource NotificationIconPath}"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="NotificationBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="NotificationOverviewBadges" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="NotificationRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="NotificationRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="NotificationDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="NotificationDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="NotificationCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="NotificationCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                            <!-- Sound Card -->
                            <controls:SettingsCard
                                Description="{x:Bind ViewModel.SoundViewModel.GroupDescriptionText}"
                                AutomationProperties.Name="{x:Bind ViewModel.SoundViewModel.DisplayName}"
                                IsClickEnabled="True"
                                Click="SoundCard_Click">
                                <controls:SettingsCard.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.SoundViewModel.DisplayName}" VerticalAlignment="Center"/>
                                        <Border x:Name="SoundNewBadge" Visibility="Collapsed"
                                                Style="{StaticResource NewBadgeBorderStyle}">
                                            <TextBlock x:Name="SoundNewText" Style="{StaticResource NewBadgeTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </controls:SettingsCard.Header>
                                <controls:SettingsCard.HeaderIcon>
                                    <fluentIcons:FluentIcon Icon="Speaker2" IconVariant="Regular"/>
                                </controls:SettingsCard.HeaderIcon>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <InfoBadge x:Name="SoundBadge" Visibility="Collapsed"/>
                                    <StackPanel x:Name="SoundOverviewBadges" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                                        <Border x:Name="SoundRecommendedPill" Style="{StaticResource BadgeRecommendedStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon Glyph="&#xE735;" FontSize="12" VerticalAlignment="Center" Margin="0,1,0,0"
                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                <TextBlock x:Name="SoundRecommendedText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="SoundDefaultPill" Style="{StaticResource BadgeDefaultStyle}">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <PathIcon Data="{StaticResource BadgeDefaultIconPath}" Height="12" Width="12"
                                                          VerticalAlignment="Center" Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                                <TextBlock x:Name="SoundDefaultText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeDefaultForeground}"/>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="SoundCustomPill" Style="{StaticResource BadgeCustomStyle}" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock x:Name="SoundCustomText" Style="{StaticResource BadgeTextStyle}"
                                                           Foreground="{ThemeResource BadgeCustomForeground}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </controls:SettingsCard>

                        </StackPanel>
                        </Grid>
                    </ScrollView>
                </Grid>

                <!-- Detail Page Frame (visible when on detail page) -->
                <Frame
                    x:Name="InnerContentFrame"
                    Visibility="Collapsed"
                    Navigated="InnerContentFrame_Navigated"/>
            </Grid>
        </Border>
    </Grid>
</Page>
