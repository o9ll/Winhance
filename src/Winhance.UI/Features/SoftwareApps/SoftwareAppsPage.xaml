<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winhance.UI.Features.SoftwareApps.SoftwareAppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:primitives="using:CommunityToolkit.WinUI.UI.Controls.Primitives"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DefaultBindMode="OneWay"
    NavigationCacheMode="Enabled"
    xmlns:fluentIcons="using:FluentIcons.WinUI"
    xmlns:vm="using:Winhance.UI.Features.SoftwareApps.ViewModels"
    xmlns:winhanceControls="using:Winhance.UI.Features.Common.Controls"
    xmlns:helpers="using:Winhance.UI.Features.Common.Helpers"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- ============================================================
             Icon sizing knobs (Software & Apps tabs).
             Each view has its OWN icon-size pair — they are NOT shared:
               • CardIconSize / CardFallbackFontSize        → Compact view
                 (Windows Apps + External Apps compact lists)
               • DataGridIconSize / DataGridFallbackFontSize → Table view
               • CardViewIconSize / CardViewFallbackFontSize → Card view
                 (defined further down)
             FontIcon size is the fallback glyph that shows when no real
             icon is resolved; ~half the icon size is a good default.
             Also tune AppItemViewModel.cs `DecodePixelWidth` (≈ 2× your
             largest icon size) for HiDPI sharpness.
             ============================================================ -->
        <x:Double x:Key="CardIconSize">20</x:Double>
        <x:Double x:Key="CardFallbackFontSize">16</x:Double>
        <x:Double x:Key="DataGridIconSize">20</x:Double>
        <x:Double x:Key="DataGridFallbackFontSize">12</x:Double>

        <!-- FluentIcon "Apps Color" carries intrinsic padding inside its draw
             box, so it renders a little smaller than its nominal FontSize.
             These size the Apps fallback specifically. Keep each value ≤ its
             matching icon size (CardAppsIconSize ≤ CardIconSize, and
             DataGridAppsIconSize ≤ DataGridIconSize) so the fallback never
             overflows its square icon cell — the intrinsic padding still lands
             the visible glyph close to the puzzle/gear FontIcon fallbacks. -->
        <x:Double x:Key="CardAppsIconSize">20</x:Double>
        <x:Double x:Key="DataGridAppsIconSize">20</x:Double>

        <!-- Card-view cell width (fixed). UniformWrapPanel renders every cell
             at exactly this width and wraps when the next cell would overflow,
             so cards never grow as you widen the window — leftover row space
             is just empty padding on the right. Column count:
                 cols = floor((panel_width + ColumnSpacing) / (CardItemWidth + ColumnSpacing))
             Bigger value → fewer columns. -->
        <x:Double x:Key="CardItemWidth">420</x:Double>
        <!-- Compact-view cell width (fixed). Like CardItemWidth, the compact
             ItemsControl + UniformWrapPanel renders every row at exactly this
             width and wraps when the next would overflow; the containing
             StackPanel is centred via a code-behind MaxWidth clamp so leftover
             space becomes symmetric outer margin (Microsoft Store pattern).
             Kept narrower than the Card view so a wide window still fits
             several columns of single-line rows. -->
        <x:Double x:Key="CompactItemWidth">320</x:Double>
        <!-- Compact-view name TextBlock MaxWidth cap. The compact row uses an
             auto-sized Grid [icons, name, warning] so the warning icon sits
             immediately to the right of the name's rendered text (rather than
             being pushed to the far right of the cell). The name TextBlock
             gets MaxWidth=CompactNameMaxWidth — that's what triggers
             CharacterEllipsis when a long name would otherwise expand the
             auto column. Sized so icons (~76dp) + this (210dp) + warning
             (16dp) + spacings + CheckBox padding fits inside CompactItemWidth
             (320dp). Scaled in lockstep with CompactItemWidth at startup via
             ApplyTextScaling so higher text scale gets proportionally more
             room for the name. -->
        <x:Double x:Key="CompactNameMaxWidth">210</x:Double>
        <x:Double x:Key="CardViewIconSize">36</x:Double>
        <!-- FontIcon fallback glyph FontSize (puzzle for Capability, gear for
             Optional Feature). Real card icons fill the 36×36 container; FontIcons
             render ~70-75% of their FontSize as visible glyph, so 28 lands the
             visible glyph at ~20px — comparable to the real-icon weight. -->
        <x:Double x:Key="CardViewFallbackFontSize">28</x:Double>
        <!-- FluentIcon "Apps" (color variant) needs more FontSize than the
             FontIcons above because the colored variant has extra intrinsic
             padding around its draw box (same gap that exists in Compact view
             between CardFallbackFontSize and CardAppsIconSize). +10 keeps
             visible Apps glyph weight in line with the puzzle/gear glyphs. -->
        <x:Double x:Key="CardViewAppsIconSize">38</x:Double>
        <CornerRadius x:Key="CardViewCornerRadius">8</CornerRadius>
        <Thickness x:Key="CardViewPadding">12,10</Thickness>

        <!-- Card view sizing. UniformWrapPanel forces every cell to CardCellHeight
             so cards are uniform across rows. CardContentHeight sizes the inner
             content area; the difference (~24px) is Border padding (10+10) plus
             border thickness (1+1, or 2+2 when Checked).
             Tuned so a 2-line description fits comfortably with the title row,
             with a couple px of breathing room when only one line is rendered.
             Title row ≈ 22px + 4px margin + 2-line desc ≈ 32px = 58px content. -->
        <x:Double x:Key="CardContentHeight">84</x:Double>
        <x:Double x:Key="CardCellHeight">108</x:Double>
        <!-- Pill geometry. CornerRadius is set high enough to fully round the ends
             at the rendered pill height (~18px), but kept finite so it clips
             cleanly at HiDPI scales (CornerRadius=999 can render with subpixel
             artefacts on the inner border). -->
        <CornerRadius x:Key="CardPillCornerRadius">10</CornerRadius>
        <Thickness x:Key="CardPillPadding">8,2</Thickness>

        <!-- Card-view selection target. Wrapping the whole card in a templated
             ToggleButton is what makes "click anywhere on the card" reliably
             toggle selection — Border + Tapped from the data template was
             not firing in this hierarchy (ItemsRepeater + StackPanel + ScrollViewer).
             The ToggleButton's IsChecked is bound two-way to AppItemViewModel.IsSelected,
             so the inner CheckBox visual indicator only needs OneWay binding and
             can stay IsHitTestVisible=False (no double-toggle). -->
        <Style x:Key="CardToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="CardBorder"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource CardViewCornerRadius}"
                                Padding="{StaticResource CardViewPadding}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ControlStrongStrokeColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="2"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="CheckedPointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="2"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="CheckedPressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="2"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="CheckedDisabled">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0"/>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- First column header: left-rounded corners with select-all checkbox -->
        <Style x:Key="FirstColumnHeaderStyle" TargetType="primitives:DataGridColumnHeader">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="primitives:DataGridColumnHeader">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="HeaderBackground"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    CornerRadius="4,0,0,4"/>
                            <CheckBox Tag="SelectAll"
                                      MinWidth="20"
                                      Margin="8,4,4,4"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Middle column headers: no rounding, with sort indicators -->
        <Style x:Key="ColumnHeaderStyle" TargetType="primitives:DataGridColumnHeader">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8,12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="primitives:DataGridColumnHeader">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SortStates">
                                    <VisualState x:Name="Unsorted"/>
                                    <VisualState x:Name="SortAscending">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Glyph">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="&#xE70E;"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="SortDescending">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Glyph">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="&#xE70D;"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="HeaderBackground"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    CornerRadius="0"/>
                            <Grid Padding="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                                <FontIcon x:Name="SortIcon"
                                          Grid.Column="1"
                                          Glyph="&#xE70E;"
                                          FontSize="10"
                                          Margin="4,0,0,0"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Last column header: right-rounded corners, with sort indicators -->
        <Style x:Key="LastColumnHeaderStyle" TargetType="primitives:DataGridColumnHeader">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8,12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="primitives:DataGridColumnHeader">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderBackground" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorDisabledBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SortStates">
                                    <VisualState x:Name="Unsorted"/>
                                    <VisualState x:Name="SortAscending">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Glyph">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="&#xE70E;"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="SortDescending">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SortIcon" Storyboard.TargetProperty="Glyph">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="&#xE70D;"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="HeaderBackground"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    CornerRadius="0,4,4,0"/>
                            <Grid Padding="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                                <FontIcon x:Name="SortIcon"
                                          Grid.Column="1"
                                          Glyph="&#xE70E;"
                                          FontSize="10"
                                          Margin="4,0,0,0"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Spacer column header: right-rounded corners with card background -->
        <Style x:Key="SpacerColumnHeaderStyle" TargetType="primitives:DataGridColumnHeader">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="primitives:DataGridColumnHeader">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="0,4,4,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Fix 2: Remove header-to-row separator line -->
        <SolidColorBrush x:Key="GridLinesBrush" Color="Transparent"/>

        <!-- Fix 3A: Remove cell focus visuals (white/black rectangles on click) -->
        <SolidColorBrush x:Key="DataGridCellFocusVisualPrimaryBrush" Color="Transparent"/>
        <SolidColorBrush x:Key="DataGridCellFocusVisualSecondaryBrush" Color="Transparent"/>
        <SolidColorBrush x:Key="DataGridCurrencyVisualPrimaryBrush" Color="Transparent"/>

        <!-- Fix 3B: Custom DataGridRow style with accent selection border.
             Uses standard WinUI 3 brushes instead of DataGrid-internal theme resources
             (DataGridRow*BackgroundColor etc. are not in scope at page level). -->
        <Style x:Key="SelectionRowStyle" TargetType="controls:DataGridRow">
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:DataGridRow">
                        <primitives:DataGridFrozenGrid x:Name="RowRoot">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="NormalAlternatingRow"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Fill">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="NormalSelected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Fill">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBorder"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOverSelected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Fill">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBorder"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOverUnfocusedSelected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Fill">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBorder"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="UnfocusedSelected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Fill">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBorder"
                                                                           Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="ValidationStates">
                                    <VisualState x:Name="Valid"/>
                                    <VisualState x:Name="Invalid">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="0"
                                                                           Storyboard.TargetName="BackgroundRectangle"
                                                                           Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <DoubleAnimation Storyboard.TargetName="InvalidVisualElement"
                                                             Storyboard.TargetProperty="Opacity"
                                                             Duration="0"
                                                             To="0.4"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Rectangle x:Name="BackgroundRectangle" Grid.ColumnSpan="2"
                                       Fill="Transparent"/>
                            <Rectangle x:Name="InvalidVisualElement" Grid.ColumnSpan="2"
                                       Opacity="0"
                                       Fill="{ThemeResource SystemFillColorCriticalBrush}"/>

                            <!-- Accent selection border overlay -->
                            <Border x:Name="SelectionBorder"
                                    Grid.ColumnSpan="2"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Margin="0,0,4,0"
                                    BorderBrush="Transparent"/>

                            <primitives:DataGridRowHeader x:Name="RowHeader" Grid.RowSpan="3"
                                                          primitives:DataGridFrozenGrid.IsFrozen="True"/>
                            <primitives:DataGridCellsPresenter x:Name="CellsPresenter" Grid.Column="1"
                                                                primitives:DataGridFrozenGrid.IsFrozen="True"
                                                                MinHeight="32"
                                                                AutomationProperties.AccessibilityView="Raw"/>
                            <primitives:DataGridDetailsPresenter x:Name="DetailsPresenter" Grid.Row="1" Grid.Column="1"
                                                                  Background="Transparent"
                                                                  AutomationProperties.AccessibilityView="Raw"/>
                            <Rectangle x:Name="BottomGridLine" Grid.Row="2" Grid.Column="1"
                                       HorizontalAlignment="Stretch" Height="1"/>
                        </primitives:DataGridFrozenGrid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="AppCardTemplate" x:DataType="vm:AppItemViewModel">
            <!-- Wrap the whole card in a templated ToggleButton so clicking
                 anywhere on it toggles selection. ToggleButton.IsChecked
                 binds two-way to IsSelected; the inner CheckBox is purely a
                 visual indicator (IsHitTestVisible=False, OneWay). -->
            <ToggleButton IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                          Style="{StaticResource CardToggleButtonStyle}"
                          AutomationProperties.Name="{x:Bind Name}">
                <!-- MinHeight (not Height) so the inner content can grow past
                     the default 84dp when Windows' "Make text bigger" scale is
                     applied; UniformWrapPanel treats its ItemHeight as a floor
                     too, so the surrounding cell grows in lockstep. At 100%
                     text scale every card stays at the original 84dp/108dp. -->
                <Grid ColumnSpacing="12" MinHeight="{StaticResource CardContentHeight}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0"
                              IsChecked="{x:Bind IsSelected, Mode=OneWay}"
                              IsHitTestVisible="False"
                              IsTabStop="False"
                              MinWidth="0"
                              VerticalAlignment="Center"/>

                    <Grid Grid.Column="1"
                          Width="{StaticResource CardViewIconSize}"
                          Height="{StaticResource CardViewIconSize}"
                          VerticalAlignment="Center">
                        <Image Source="{x:Bind IconSource, Mode=OneWay}"
                               Stretch="Uniform"
                               Visibility="{x:Bind HasIcon, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                FontSize="{StaticResource CardViewAppsIconSize}"
                                                Visibility="{x:Bind IsAppXFallback, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <FontIcon Glyph="&#xE9F9;"
                                  FontSize="{StaticResource CardViewFallbackFontSize}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind IsCapabilityFallback, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <FontIcon Glyph="&#xE713;"
                                  FontSize="{StaticResource CardViewFallbackFontSize}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind IsOptionalFeatureFallback, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </Grid>

                    <!-- Content: name row, description, then status badges below
                         the description — mirrors the Optimize/Customize settings
                         card layout. Name row no longer shares space with the pills,
                         so long names no longer get clipped. -->
                    <Grid Grid.Column="2" VerticalAlignment="Center" MinWidth="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0: name + website button -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{x:Bind Name}"
                                       FontWeight="SemiBold"
                                       FontSize="14"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"/>
                            <Button Grid.Column="1"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="4"
                                    MinWidth="0"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind HasWebsiteUrl, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    Click="WebsiteButton_Click"
                                    Tag="{x:Bind WebsiteUrl}"
                                    ToolTipService.ToolTip="{x:Bind WebsiteUrl}"
                                    AutomationProperties.Name="{x:Bind OpenWebsiteAutomationName, Mode=OneWay}">
                                <Viewbox Width="16" Height="16">
                                    <fluentIcons:FluentIcon Icon="Open" IconVariant="Regular"/>
                                </Viewbox>
                            </Button>
                        </Grid>

                        <!-- Row 1: description -->
                        <TextBlock Grid.Row="1"
                                   Text="{x:Bind Description}"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="2"
                                   Margin="0,2,0,0"
                                   Visibility="{x:Bind HasDescription, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- Row 2: status badges. StackPanel.Spacing only applies
                             between visible children, so collapsed pills leave no gap. -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="6" Margin="0,6,0,0">
                            <Border Style="{StaticResource BadgeInstalledStyle}"
                                    Visibility="{x:Bind IsInstalled, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Viewbox Width="14" Height="14" Margin="0,0,4,2" VerticalAlignment="Center">
                                        <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                  Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                    </Viewbox>
                                    <TextBlock Text="{x:Bind InstalledStatusText, Mode=OneWay}"
                                               Style="{StaticResource BadgeTextStyle}"
                                               Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource BadgeNonReinstallableStyle}"
                                    Visibility="{x:Bind ShowNonReinstallableChip, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Viewbox Width="12" Height="12" Margin="0,0,4,2" VerticalAlignment="Center">
                                        <PathIcon Data="{StaticResource DeleteForeverIconPath}"
                                                  Foreground="{ThemeResource BadgeDangerForeground}"/>
                                    </Viewbox>
                                    <TextBlock Text="{x:Bind ReinstallableStatusText, Mode=OneWay}"
                                               Style="{StaticResource BadgeTextStyle}"
                                               Foreground="{ThemeResource BadgeDangerForeground}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource BadgeWarningStyle}"
                                    Visibility="{x:Bind HasInstabilityWarning, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    ToolTipService.ToolTip="{x:Bind InstabilityWarningTooltip, Mode=OneWay}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Viewbox Width="12" Height="12" Margin="0,0,4,2" VerticalAlignment="Center">
                                        <PathIcon Data="{StaticResource AlertIconPath}"
                                                  Foreground="{ThemeResource BadgeCustomForeground}"/>
                                    </Viewbox>
                                    <TextBlock Text="{x:Bind InstabilityWarningLabel, Mode=OneWay}"
                                               Style="{StaticResource BadgeTextStyle}"
                                               Foreground="{ThemeResource BadgeCustomForeground}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>
            </ToggleButton>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="0,0,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Action Bar -->
            <RowDefinition Height="*"/>     <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Row 0: Page Header with Icon -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16">
            <Viewbox Width="64" Height="64" Stretch="Uniform">
                <PathIcon Data="{StaticResource SoftwareAppsIconPath}" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </Viewbox>
            <StackPanel VerticalAlignment="Center">
                <TextBlock
                    Text="{x:Bind ViewModel.PageTitle}"
                    Style="{StaticResource TitleTextBlockStyle}"
                    FontWeight="Bold"
                    FontSize="30"
                    AutomationProperties.HeadingLevel="Level1"/>
                <TextBlock
                    Text="{x:Bind ViewModel.PageDescription}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Row 1: Action Bar (right-aligned) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,12,0,16">
            <!-- Normal mode: Install Button -->
            <Button Command="{x:Bind ViewModel.InstallSelectedItemsCommand}"
                    IsEnabled="{x:Bind ViewModel.CanInstallItems, Mode=OneWay}"
                    AutomationProperties.Name="{x:Bind ViewModel.InstallButtonText}"
                    Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="ArrowDownload" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.InstallButtonText}"/>
                </StackPanel>
            </Button>
            <!-- Normal mode: Remove Button -->
            <Button Command="{x:Bind ViewModel.RemoveSelectedItemsCommand}"
                    IsEnabled="{x:Bind ViewModel.CanRemoveItems, Mode=OneWay}"
                    AutomationProperties.Name="{x:Bind ViewModel.RemoveButtonText, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="Delete" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.RemoveButtonText, Mode=OneWay}"/>
                </StackPanel>
            </Button>
            <!-- Review mode: Install action toggle -->
            <ToggleButton x:Name="InstallActionToggle"
                          Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                          IsChecked="{x:Bind ViewModel.CurrentInstallAction, Mode=TwoWay}"
                          AutomationProperties.Name="{x:Bind ViewModel.InstallButtonText}"
                          Padding="10,6,12,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="ArrowDownload" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.InstallButtonText}"/>
                </StackPanel>
            </ToggleButton>
            <!-- Review mode: Remove action toggle -->
            <ToggleButton x:Name="RemoveActionToggle"
                          Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                          IsChecked="{x:Bind ViewModel.CurrentRemoveAction, Mode=TwoWay}"
                          AutomationProperties.Name="{x:Bind ViewModel.RemoveButtonText, Mode=OneWay}"
                          Padding="10,6,12,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="Delete" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.RemoveButtonText, Mode=OneWay}"/>
                </StackPanel>
            </ToggleButton>
            <AppBarSeparator/>
            <Button Command="{x:Bind ViewModel.RefreshInstallationStatusCommand}"
                    AutomationProperties.Name="{x:Bind ViewModel.RefreshButtonText}">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F5"/>
                </Button.KeyboardAccelerators>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="ArrowSync" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.RefreshButtonText}"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.ShowHelpCommand}"
                    AutomationProperties.Name="{x:Bind ViewModel.HelpButtonText}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <fluentIcons:FluentIcon Icon="QuestionCircle" IconVariant="Regular" FontSize="18"/>
                    <TextBlock Text="{x:Bind ViewModel.HelpButtonText}"/>
                </StackPanel>
            </Button>
            <!-- View Mode Toggle -->
            <Border CornerRadius="4"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <ToggleButton x:Name="CardViewToggle"
                                  IsChecked="{x:Bind ViewModel.IsCardView, Mode=OneWay}"
                                  Click="CardViewToggle_Click"
                                  ToolTipService.ToolTip="{x:Bind ViewModel.ViewModeCardTooltip}"
                                  AutomationProperties.Name="{x:Bind ViewModel.ViewModeCardTooltip}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  CornerRadius="4,0,0,4"
                                  Padding="10,6">
                        <fluentIcons:FluentIcon Icon="Grid" IconVariant="Regular" FontSize="18"/>
                    </ToggleButton>
                    <ToggleButton x:Name="TableViewToggle"
                                  IsChecked="{x:Bind ViewModel.IsTableView, Mode=OneWay}"
                                  Click="TableViewToggle_Click"
                                  ToolTipService.ToolTip="{x:Bind ViewModel.ViewModeTableTooltip}"
                                  AutomationProperties.Name="{x:Bind ViewModel.ViewModeTableTooltip}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  CornerRadius="0"
                                  Padding="10,6">
                        <fluentIcons:FluentIcon Icon="TableSimple" IconVariant="Regular" FontSize="18"/>
                    </ToggleButton>
                    <ToggleButton x:Name="CompactViewToggle"
                                  IsChecked="{x:Bind ViewModel.IsCompactView, Mode=OneWay}"
                                  Click="CompactViewToggle_Click"
                                  ToolTipService.ToolTip="{x:Bind ViewModel.ViewModeCompactTooltip}"
                                  AutomationProperties.Name="{x:Bind ViewModel.ViewModeCompactTooltip}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  CornerRadius="0,4,4,0"
                                  Padding="10,6">
                        <fluentIcons:FluentIcon Icon="LineHorizontal3" IconVariant="Regular" FontSize="18"/>
                    </ToggleButton>
                </StackPanel>
            </Border>
            <AutoSuggestBox
                PlaceholderText="{x:Bind ViewModel.SearchPlaceholder}"
                helpers:AutoSuggestBoxExtensions.PlaceholderEllipsis="True"
                QueryIcon="Find"
                Width="220"
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Row 2: Content Container with rounded corners and background -->
        <Border
            Grid.Row="2"
            Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}"
            CornerRadius="8"
            Margin="0,0,0,4">
            <Grid>
                <!-- Tab Navigation Container -->
                <Border Margin="12,12,12,0"
                        VerticalAlignment="Top"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="6"
                        Padding="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Windows Apps Tab -->
                        <Grid Grid.Column="0">
                            <!-- Selection background (visible when selected) -->
                            <Border CornerRadius="4"
                                    Margin="2"
                                    Background="{ThemeResource ControlFillColorDefaultBrush}"
                                    BorderThickness="1"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    IsHitTestVisible="False"
                                    Visibility="{x:Bind ViewModel.IsWindowsAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <!-- Accent indicator on left -->
                            <Border Width="3"
                                    Height="18"
                                    CornerRadius="1.5"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="6,0,0,0"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    IsHitTestVisible="False"
                                    Visibility="{x:Bind ViewModel.IsWindowsAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <!-- Tab button (transparent) -->
                            <Button x:Name="WindowsAppsTab"
                                    Command="{x:Bind ViewModel.SelectWindowsAppsTabCommand}"
                                    AutomationProperties.Name="{x:Bind ViewModel.WindowsAppsTabText}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="12,10">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Viewbox Width="18" Height="18">
                                        <PathIcon Data="{StaticResource WindowsLogoIconPath}" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    </Viewbox>
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsTabText}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <!-- Review mode badge -->
                            <InfoBadge x:Name="WindowsAppsTabBadge"
                                       Value="{x:Bind ViewModel.WindowsAppsSelectedCount, Mode=OneWay}"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Top"
                                       Margin="0,2,8,0"
                                       Visibility="Collapsed"/>
                        </Grid>

                        <!-- Separator -->
                        <Border Grid.Column="1"
                                Width="1"
                                Margin="4,8"
                                VerticalAlignment="Stretch"
                                Background="{ThemeResource ControlStrokeColorDefaultBrush}"/>

                        <!-- External Apps Tab -->
                        <Grid Grid.Column="2">
                            <!-- Selection background (visible when selected) -->
                            <Border CornerRadius="4"
                                    Margin="2"
                                    Background="{ThemeResource ControlFillColorDefaultBrush}"
                                    BorderThickness="1"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    IsHitTestVisible="False"
                                    Visibility="{x:Bind ViewModel.IsExternalAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <!-- Accent indicator on left -->
                            <Border Width="3"
                                    Height="18"
                                    CornerRadius="1.5"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="6,0,0,0"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    IsHitTestVisible="False"
                                    Visibility="{x:Bind ViewModel.IsExternalAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <!-- Tab button (transparent) -->
                            <Button x:Name="ExternalAppsTab"
                                    Command="{x:Bind ViewModel.SelectExternalAppsTabCommand}"
                                    AutomationProperties.Name="{x:Bind ViewModel.ExternalAppsTabText}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="12,10">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Viewbox Width="18" Height="18">
                                        <PathIcon Data="{StaticResource ExternalAppsIconPath}" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    </Viewbox>
                                    <TextBlock Text="{x:Bind ViewModel.ExternalAppsTabText}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <!-- Review mode badge -->
                            <InfoBadge x:Name="ExternalAppsTabBadge"
                                       Value="{x:Bind ViewModel.ExternalAppsSelectedCount, Mode=OneWay}"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Top"
                                       Margin="0,2,8,0"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Content Area (below tabs) -->
                <Grid Margin="14,70,12,0">
                    <!-- Windows Apps Content -->
                    <Grid Visibility="{x:Bind ViewModel.IsWindowsAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Review Mode Banner for Windows Apps -->
                        <InfoBar Grid.Row="0"
                                 x:Name="WindowsAppsReviewBanner"
                                 IsOpen="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay}"
                                 Severity="Warning"
                                 IsClosable="False"
                                 Message="{x:Bind ViewModel.ReviewWindowsAppsBannerText, Mode=OneWay}"
                                 Margin="0,0,0,8"
                                 Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- DataGrid View -->
                        <controls:DataGrid x:Name="WindowsAppsDataGrid"
                                           Grid.Row="1"
                                           Visibility="{x:Bind ViewModel.IsTableView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           Loaded="WindowsAppsDataGrid_Loaded"
                                           ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.ItemsView, Mode=OneWay}"
                                           AutoGenerateColumns="False"
                                           CanUserSortColumns="True"
                                           CanUserReorderColumns="False"
                                           CanUserResizeColumns="True"
                                           GridLinesVisibility="None"
                                           HeadersVisibility="Column"
                                           SelectionMode="Extended"
                                           IsReadOnly="True"
                                           VerticalScrollBarVisibility="Auto"
                                           HorizontalScrollBarVisibility="Disabled"
                                           Background="Transparent"
                                           RowBackground="Transparent"
                                           AlternatingRowBackground="Transparent"
                                           BorderThickness="0"
                                           Padding="0,0,0,0"
                                           ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                                           RowStyle="{StaticResource SelectionRowStyle}"
                                           Sorting="DataGrid_Sorting">
                            <controls:DataGrid.Columns>
                                <!-- Selection Column with Select All header -->
                                <controls:DataGridTemplateColumn Width="40" CanUserResize="False" IsReadOnly="False"
                                                                  HeaderStyle="{StaticResource FirstColumnHeaderStyle}">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      AutomationProperties.Name="{Binding Name}"
                                                      MinWidth="20"
                                                      HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                    <controls:DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      AutomationProperties.Name="{Binding Name}"
                                                      MinWidth="20"
                                                      HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellEditingTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Name Column -->
                                <controls:DataGridTemplateColumn Header="Name" Width="*" Tag="Name">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                                <!-- Icon size driven by DataGridIconSize / DataGridFallbackFontSize resources at the top of the page. -->
                                                <Grid Width="{StaticResource DataGridIconSize}" Height="{StaticResource DataGridIconSize}"
                                                      VerticalAlignment="Center" Margin="-4,0,0,0">
                                                    <Image Source="{Binding IconSource}"
                                                           Width="{StaticResource DataGridIconSize}" Height="{StaticResource DataGridIconSize}"
                                                           Stretch="Uniform"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                            FontSize="{StaticResource DataGridAppsIconSize}"
                                                                            Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <FontIcon Glyph="&#xE9F9;" FontSize="{StaticResource DataGridFallbackFontSize}"
                                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                              Visibility="{Binding IsCapabilityFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <FontIcon Glyph="&#xE713;" FontSize="{StaticResource DataGridFallbackFontSize}"
                                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                              Visibility="{Binding IsOptionalFeatureFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                </Grid>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                                <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                         Visibility="{Binding HasInstabilityWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                         ToolTipService.ToolTip="{Binding InstabilityWarningTooltip}">
                                                    <PathIcon Data="{StaticResource AlertIconPath}"
                                                              Foreground="{ThemeResource BadgeCustomForeground}"/>
                                                </Viewbox>
                                            </StackPanel>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Type Column -->
                                <controls:DataGridTemplateColumn Header="Type" Width="*" Tag="ItemTypeDescription">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ItemTypeDescription}" VerticalAlignment="Center"
                                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Status Column -->
                                <controls:DataGridTemplateColumn Header="Status" Width="*" Tag="IsInstalled">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                         Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                              Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                </Viewbox>
                                                <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                         Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                              Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                </Viewbox>
                                                <TextBlock Text="{Binding InstalledStatusText}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Reinstallable Column (last visible) -->
                                <controls:DataGridTemplateColumn Header="Installable" Width="*" Tag="CanBeReinstalled"
                                                                  HeaderStyle="{StaticResource LastColumnHeaderStyle}">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                         Visibility="{Binding CanBeReinstalled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DownloadIconPath}"
                                                              Foreground="{ThemeResource BadgePreferenceForeground}"/>
                                                </Viewbox>
                                                <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                         Visibility="{Binding CanBeReinstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DeleteForeverIconPath}"
                                                              Foreground="{ThemeResource BadgeDangerForeground}"/>
                                                </Viewbox>
                                                <TextBlock Text="{Binding ReinstallableStatusText}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>

                        <!-- Card View for Windows Apps. The inner StackPanel is centred
                             horizontally with a MaxWidth set in code-behind to exactly
                             cols × CardItemWidth + (cols-1) × ColumnSpacing, so the
                             section headers, Select-all checkboxes, and the card grid
                             all share the same horizontal extents. -->
                        <ScrollViewer Grid.Row="1" x:Name="WindowsAppsCardScrollViewer"
                                      Visibility="{x:Bind ViewModel.IsCardView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="0,0,0,12"
                                      SizeChanged="WindowsAppsCardScrollViewer_SizeChanged">
                            <StackPanel x:Name="WindowsAppsCardContentStack"
                                        Spacing="20"
                                        Padding="0,8,0,8"
                                        HorizontalAlignment="Center">
                                <!-- Selection Checkboxes -->
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelected, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelectedInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllInstalledCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllNotInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelectedNotInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllNotInstalledCommand}"/>
                                </StackPanel>

                                <!-- Apps Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionAppsHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <!-- Non-virtualising ItemsControl + custom UniformWrapPanel.
                                         ItemsRepeater + UniformGridLayout produced a measure cycle
                                         that snapped the outer ScrollViewer to the top. UniformWrapPanel
                                         renders every cell at exactly CardItemWidth, wrapping when
                                         the next would overflow — predictable, no cycle. -->
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.WindowsAppsFiltered, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource AppCardTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel
                                                    ItemWidth="{StaticResource CardItemWidth}"
                                                    ItemHeight="{StaticResource CardCellHeight}"
                                                    ColumnSpacing="16"
                                                    RowSpacing="16"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Capabilities Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionCapabilitiesHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.CapabilitiesFiltered, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource AppCardTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel
                                                    ItemWidth="{StaticResource CardItemWidth}"
                                                    ItemHeight="{StaticResource CardCellHeight}"
                                                    ColumnSpacing="16"
                                                    RowSpacing="16"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Optional Features Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionOptionalFeaturesHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.OptionalFeaturesFiltered, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource AppCardTemplate}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel
                                                    ItemWidth="{StaticResource CardItemWidth}"
                                                    ItemHeight="{StaticResource CardCellHeight}"
                                                    ColumnSpacing="16"
                                                    RowSpacing="16"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Compact View for Windows Apps -->
                        <ScrollViewer x:Name="WindowsAppsCompactScrollViewer"
                                      Grid.Row="1"
                                      Visibility="{x:Bind ViewModel.IsCompactView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="0,0,0,12"
                                      SizeChanged="WindowsAppsCompactScrollViewer_SizeChanged">
                            <!-- Centred + MaxWidth-clamped in code-behind (UpdateCompactContentMaxWidth)
                                 so headers, Select-all checkboxes and the row grid share one column —
                                 same Microsoft Store pattern as the card view above. -->
                            <StackPanel x:Name="WindowsAppsCompactContentStack"
                                        Spacing="16" Padding="0,8,0,8"
                                        HorizontalAlignment="Center">
                                <!-- Selection Checkboxes -->
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelected, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelectedInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllInstalledCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.WindowsAppsViewModel.SelectAllNotInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.WindowsAppsViewModel.IsAllSelectedNotInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.WindowsAppsViewModel.ToggleSelectAllNotInstalledCommand}"/>
                                </StackPanel>

                                <!-- Windows Apps Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionAppsHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.WindowsAppsFiltered, Mode=OneWay}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel ItemWidth="{StaticResource CompactItemWidth}" ColumnSpacing="8" RowSpacing="2"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" MinWidth="0" Padding="10,3" FontSize="16"
                                                         AutomationProperties.Name="{Binding Name}">
                                                    <!-- Auto-sized Grid (NOT stretched to the cell) restores the
                                                         pre-fix visual: each row's content sits at its natural width
                                                         and the UniformWrapPanel slot's leftover space is empty on the
                                                         right, just like the Microsoft Store / card-view pattern. The
                                                         name TextBlock has MaxWidth=CompactNameMaxWidth — that finite
                                                         cap is what makes CharacterEllipsis actually trigger (a plain
                                                         horizontal StackPanel measures every child with infinite width,
                                                         which is why long names were clipping mid-glyph before issue
                                                         #668). With the cap in place, name auto-sizes up to ~210dp and
                                                         then ellipsizes; the warning Viewbox sits immediately after,
                                                         not pinned to the far right of the cell.
                                                         Outer Margin nudges the row down to match the WinUI 3 CheckBox
                                                         glyph's built-in vertical offset (tune 3 → 2/4 if needed). -->
                                                    <Grid Margin="0,3,0,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                                                            <!-- Icon size driven by CardIconSize / CardFallbackFontSize resources at the top of the page. -->
                                                            <Grid Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                  VerticalAlignment="Center">
                                                                <Image Source="{Binding IconSource}"
                                                                       Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                       Stretch="Uniform"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                       Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                                        FontSize="{StaticResource CardAppsIconSize}"
                                                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                        Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE9F9;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsCapabilityFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE713;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsOptionalFeatureFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                            </Grid>
                                                            <!-- Installed status: green tower when installed, faded grey tower when not -->
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                            </Viewbox>
                                                            <!-- Reinstallable status: blue Download when reinstallable, red DownloadLock when not -->
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DownloadIconPath}"
                                                                          Foreground="{ThemeResource BadgePreferenceForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DeleteForeverIconPath}"
                                                                          Foreground="{ThemeResource BadgeDangerForeground}"/>
                                                            </Viewbox>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Name}"
                                                                   VerticalAlignment="Center"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   FontSize="16"
                                                                   Margin="4,0,0,0"
                                                                   MaxWidth="{StaticResource CompactNameMaxWidth}"/>
                                                        <Viewbox Grid.Column="2" Width="16" Height="16" VerticalAlignment="Center"
                                                                 Margin="4,0,0,0"
                                                                 Visibility="{Binding HasInstabilityWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                 ToolTipService.ToolTip="{Binding InstabilityWarningTooltip}">
                                                            <PathIcon Data="{StaticResource AlertIconPath}"
                                                                      Foreground="{ThemeResource BadgeCustomForeground}"/>
                                                        </Viewbox>
                                                    </Grid>
                                                </CheckBox>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Capabilities Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionCapabilitiesHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.CapabilitiesFiltered, Mode=OneWay}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel ItemWidth="{StaticResource CompactItemWidth}" ColumnSpacing="8" RowSpacing="2"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" MinWidth="0" Padding="10,3" FontSize="16"
                                                         AutomationProperties.Name="{Binding Name}">
                                                    <!-- Auto-sized Grid (NOT stretched to the cell) restores the
                                                         pre-fix visual: each row's content sits at its natural width
                                                         and the UniformWrapPanel slot's leftover space is empty on the
                                                         right, just like the Microsoft Store / card-view pattern. The
                                                         name TextBlock has MaxWidth=CompactNameMaxWidth — that finite
                                                         cap is what makes CharacterEllipsis actually trigger (a plain
                                                         horizontal StackPanel measures every child with infinite width,
                                                         which is why long names were clipping mid-glyph before issue
                                                         #668). With the cap in place, name auto-sizes up to ~210dp and
                                                         then ellipsizes; the warning Viewbox sits immediately after,
                                                         not pinned to the far right of the cell.
                                                         Outer Margin nudges the row down to match the WinUI 3 CheckBox
                                                         glyph's built-in vertical offset (tune 3 → 2/4 if needed). -->
                                                    <Grid Margin="0,3,0,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                                                            <!-- Icon size driven by CardIconSize / CardFallbackFontSize resources at the top of the page. -->
                                                            <Grid Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                  VerticalAlignment="Center">
                                                                <Image Source="{Binding IconSource}"
                                                                       Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                       Stretch="Uniform"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                       Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                                        FontSize="{StaticResource CardAppsIconSize}"
                                                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                        Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE9F9;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsCapabilityFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE713;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsOptionalFeatureFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                            </Grid>
                                                            <!-- Installed status: green tower when installed, faded grey tower when not -->
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                            </Viewbox>
                                                            <!-- Reinstallable status: blue Download when reinstallable, red DownloadLock when not -->
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DownloadIconPath}"
                                                                          Foreground="{ThemeResource BadgePreferenceForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DeleteForeverIconPath}"
                                                                          Foreground="{ThemeResource BadgeDangerForeground}"/>
                                                            </Viewbox>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Name}"
                                                                   VerticalAlignment="Center"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   FontSize="16"
                                                                   Margin="4,0,0,0"
                                                                   MaxWidth="{StaticResource CompactNameMaxWidth}"/>
                                                        <Viewbox Grid.Column="2" Width="16" Height="16" VerticalAlignment="Center"
                                                                 Margin="4,0,0,0"
                                                                 Visibility="{Binding HasInstabilityWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                 ToolTipService.ToolTip="{Binding InstabilityWarningTooltip}">
                                                            <PathIcon Data="{StaticResource AlertIconPath}"
                                                                      Foreground="{ThemeResource BadgeCustomForeground}"/>
                                                        </Viewbox>
                                                    </Grid>
                                                </CheckBox>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Optional Features Section -->
                                <StackPanel Spacing="8">
                                    <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.SectionOptionalFeaturesHeader, Mode=OneWay}"
                                               Style="{StaticResource SubtitleTextBlockStyle}"
                                               FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WindowsAppsViewModel.OptionalFeaturesFiltered, Mode=OneWay}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <winhanceControls:UniformWrapPanel ItemWidth="{StaticResource CompactItemWidth}" ColumnSpacing="8" RowSpacing="2"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" MinWidth="0" Padding="10,3" FontSize="16"
                                                         AutomationProperties.Name="{Binding Name}">
                                                    <!-- Auto-sized Grid (NOT stretched to the cell) restores the
                                                         pre-fix visual: each row's content sits at its natural width
                                                         and the UniformWrapPanel slot's leftover space is empty on the
                                                         right, just like the Microsoft Store / card-view pattern. The
                                                         name TextBlock has MaxWidth=CompactNameMaxWidth — that finite
                                                         cap is what makes CharacterEllipsis actually trigger (a plain
                                                         horizontal StackPanel measures every child with infinite width,
                                                         which is why long names were clipping mid-glyph before issue
                                                         #668). With the cap in place, name auto-sizes up to ~210dp and
                                                         then ellipsizes; the warning Viewbox sits immediately after,
                                                         not pinned to the far right of the cell.
                                                         Outer Margin nudges the row down to match the WinUI 3 CheckBox
                                                         glyph's built-in vertical offset (tune 3 → 2/4 if needed). -->
                                                    <Grid Margin="0,3,0,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                                                            <!-- Icon size driven by CardIconSize / CardFallbackFontSize resources at the top of the page. -->
                                                            <Grid Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                  VerticalAlignment="Center">
                                                                <Image Source="{Binding IconSource}"
                                                                       Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                       Stretch="Uniform"
                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                       Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                                        FontSize="{StaticResource CardAppsIconSize}"
                                                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                        Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE9F9;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsCapabilityFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                <FontIcon Glyph="&#xE713;" FontSize="{StaticResource CardFallbackFontSize}"
                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                          Visibility="{Binding IsOptionalFeatureFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                            </Grid>
                                                            <!-- Installed status: green tower when installed, faded grey tower when not -->
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                          Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                            </Viewbox>
                                                            <!-- Reinstallable status: blue Download when reinstallable, red DownloadLock when not -->
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DownloadIconPath}"
                                                                          Foreground="{ThemeResource BadgePreferenceForeground}"/>
                                                            </Viewbox>
                                                            <Viewbox Width="16" Height="16" VerticalAlignment="Center"
                                                                     Visibility="{Binding CanBeReinstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                     ToolTipService.ToolTip="{Binding ReinstallableStatusText}">
                                                                <PathIcon Data="{StaticResource DeleteForeverIconPath}"
                                                                          Foreground="{ThemeResource BadgeDangerForeground}"/>
                                                            </Viewbox>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Name}"
                                                                   VerticalAlignment="Center"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   FontSize="16"
                                                                   Margin="4,0,0,0"
                                                                   MaxWidth="{StaticResource CompactNameMaxWidth}"/>
                                                        <Viewbox Grid.Column="2" Width="16" Height="16" VerticalAlignment="Center"
                                                                 Margin="4,0,0,0"
                                                                 Visibility="{Binding HasInstabilityWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                 ToolTipService.ToolTip="{Binding InstabilityWarningTooltip}">
                                                            <PathIcon Data="{StaticResource AlertIconPath}"
                                                                      Foreground="{ThemeResource BadgeCustomForeground}"/>
                                                        </Viewbox>
                                                    </Grid>
                                                </CheckBox>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Loading overlay for Windows Apps -->
                        <Grid Grid.Row="1"
                              Visibility="{x:Bind ViewModel.WindowsAppsViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                              Background="{ThemeResource LayerFillColorDefaultBrush}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                                <ProgressRing IsActive="True" Width="48" Height="48"/>
                                <TextBlock Text="{x:Bind ViewModel.WindowsAppsViewModel.StatusText, Mode=OneWay}"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <!-- External Apps Content -->
                    <Grid Visibility="{x:Bind ViewModel.IsExternalAppsTabSelected, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Review Mode Banner for External Apps -->
                        <InfoBar Grid.Row="0"
                                 x:Name="ExternalAppsReviewBanner"
                                 IsOpen="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay}"
                                 Severity="Warning"
                                 IsClosable="False"
                                 Message="{x:Bind ViewModel.ReviewExternalAppsBannerText, Mode=OneWay}"
                                 Margin="0,0,0,8"
                                 Visibility="{x:Bind ViewModel.IsInReviewMode, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- DataGrid View -->
                        <controls:DataGrid x:Name="ExternalAppsDataGrid"
                                           Grid.Row="1"
                                           Visibility="{x:Bind ViewModel.IsTableView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           Loaded="ExternalAppsDataGrid_Loaded"
                                           ItemsSource="{x:Bind ViewModel.ExternalAppsViewModel.ItemsView, Mode=OneWay}"
                                           AutoGenerateColumns="False"
                                           CanUserSortColumns="True"
                                           CanUserReorderColumns="False"
                                           CanUserResizeColumns="True"
                                           GridLinesVisibility="None"
                                           HeadersVisibility="Column"
                                           SelectionMode="Extended"
                                           IsReadOnly="True"
                                           VerticalScrollBarVisibility="Auto"
                                           HorizontalScrollBarVisibility="Disabled"
                                           Background="Transparent"
                                           RowBackground="Transparent"
                                           AlternatingRowBackground="Transparent"
                                           BorderThickness="0"
                                           Padding="0,0,0,12"
                                           ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                                           RowStyle="{StaticResource SelectionRowStyle}"
                                           Sorting="DataGrid_Sorting">
                            <controls:DataGrid.Columns>
                                <!-- Selection Column with Select All header -->
                                <controls:DataGridTemplateColumn Width="40" CanUserResize="False" IsReadOnly="False"
                                                                  HeaderStyle="{StaticResource FirstColumnHeaderStyle}">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      AutomationProperties.Name="{Binding Name}"
                                                      MinWidth="20"
                                                      HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                    <controls:DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      AutomationProperties.Name="{Binding Name}"
                                                      MinWidth="20"
                                                      HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellEditingTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Name Column -->
                                <controls:DataGridTemplateColumn Header="Name" Width="*" Tag="Name">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                                <!-- Icon size driven by DataGridIconSize / DataGridFallbackFontSize resources at the top of the page. -->
                                                <Grid Width="{StaticResource DataGridIconSize}" Height="{StaticResource DataGridIconSize}"
                                                      VerticalAlignment="Center" Margin="-4,0,0,0">
                                                    <Image Source="{Binding IconSource}"
                                                           Width="{StaticResource DataGridIconSize}" Height="{StaticResource DataGridIconSize}"
                                                           Stretch="Uniform"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                            FontSize="{StaticResource DataGridAppsIconSize}"
                                                                            Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                </Grid>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                                <HyperlinkButton NavigateUri="{Binding WebsiteUrl}"
                                                                 Padding="0" MinHeight="0" MinWidth="0"
                                                                 VerticalAlignment="Center"
                                                                 Visibility="{Binding WebsiteUrl, Converter={StaticResource NotNullToVisibilityConverter}}"
                                                                 AutomationProperties.Name="{Binding OpenWebsiteAutomationName}">
                                                    <Viewbox Width="16" Height="16">
                                                        <fluentIcons:FluentIcon Icon="Open" IconVariant="Regular"/>
                                                    </Viewbox>
                                                </HyperlinkButton>
                                            </StackPanel>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Description Column -->
                                <controls:DataGridTemplateColumn Header="Description" Width="2*" Tag="Description">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description}" VerticalAlignment="Center"
                                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Status Column -->
                                <controls:DataGridTemplateColumn Header="Status" Width="140" Tag="IsInstalled">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                         Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                              Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                </Viewbox>
                                                <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                         Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                              Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                </Viewbox>
                                                <TextBlock Text="{Binding InstalledStatusText}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>

                                <!-- Scrollbar spacer column -->
                                <controls:DataGridTemplateColumn Width="16" CanUserResize="False" CanUserSort="False"
                                                                  HeaderStyle="{StaticResource SpacerColumnHeaderStyle}">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border/>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>

                        <!-- Card View for External Apps -->
                        <ScrollViewer Grid.Row="1" x:Name="ExternalAppsCardScrollViewer"
                                      Visibility="{x:Bind ViewModel.IsCardView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="0,0,0,12"
                                      SizeChanged="ExternalAppsCardScrollViewer_SizeChanged">
                            <StackPanel x:Name="ExternalAppsCardContentStack"
                                        Spacing="20"
                                        Padding="0,8,0,8"
                                        HorizontalAlignment="Center">
                                <!-- Selection Checkboxes -->
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelected, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelectedInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllInstalledCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllNotInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelectedNotInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllNotInstalledCommand}"/>
                                </StackPanel>

                                <!-- Categories with apps inside. Both levels switched to
                                     non-virtualising ItemsControl with a UniformWrapPanel for the
                                     inner cards — same scroll-cycle reason as Windows Apps above. -->
                                <ItemsControl ItemsSource="{x:Bind ViewModel.ExternalAppsViewModel.Categories, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Spacing="8" Margin="0,0,0,12">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           Style="{StaticResource SubtitleTextBlockStyle}"
                                                           FontWeight="SemiBold"/>
                                                <ItemsControl ItemsSource="{Binding Apps}"
                                                              ItemTemplate="{StaticResource AppCardTemplate}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <winhanceControls:UniformWrapPanel
                                                                ItemWidth="{StaticResource CardItemWidth}"
                                                                ItemHeight="{StaticResource CardCellHeight}"
                                                                ColumnSpacing="16"
                                                                RowSpacing="16"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Compact View for External Apps -->
                        <ScrollViewer x:Name="ExternalAppsCompactScrollViewer"
                                      Grid.Row="1"
                                      Visibility="{x:Bind ViewModel.IsCompactView, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="0,0,0,12"
                                      SizeChanged="ExternalAppsCompactScrollViewer_SizeChanged">
                            <!-- Centred + MaxWidth-clamped in code-behind (UpdateCompactContentMaxWidth)
                                 so the Select-all checkboxes and the category Expanders share one
                                 centred column — same Microsoft Store pattern as the card view. -->
                            <StackPanel x:Name="ExternalAppsCompactContentStack"
                                        Spacing="12" Padding="0,8,0,8"
                                        HorizontalAlignment="Center">
                                <!-- Selection Checkboxes -->
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelected, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelectedInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllInstalledCommand}"/>
                                    <CheckBox Content="{x:Bind ViewModel.ExternalAppsViewModel.SelectAllNotInstalledLabel, Mode=OneWay}" MinWidth="0"
                                              IsChecked="{x:Bind ViewModel.ExternalAppsViewModel.IsAllSelectedNotInstalled, Mode=OneWay}"
                                              Command="{x:Bind ViewModel.ExternalAppsViewModel.ToggleSelectAllNotInstalledCommand}"/>
                                </StackPanel>

                                <!-- Categories -->
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.ExternalAppsViewModel.Categories, Mode=OneWay}">
                                    <ItemsRepeater.Layout>
                                        <StackLayout Spacing="8"/>
                                    </ItemsRepeater.Layout>
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <Expander IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                                                     AutomationProperties.Name="{Binding DisplayName}">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                                        <FontIcon Glyph="{Binding IconGlyph}" FontSize="16"/>
                                                        <TextBlock Text="{Binding DisplayName}"
                                                                   FontWeight="SemiBold" FontSize="14"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <Expander.Content>
                                                    <ItemsControl ItemsSource="{Binding Apps}" Margin="0,4,0,4">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <winhanceControls:UniformWrapPanel ItemWidth="{StaticResource CompactItemWidth}" ColumnSpacing="8" RowSpacing="2"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" MinWidth="0" Padding="10,3" FontSize="16"
                                                                         AutomationProperties.Name="{Binding Name}">
                                                                    <!-- Auto-sized Grid + MaxWidth-bound name TextBlock — see the
                                                                         matching comment on the Windows Apps compact template
                                                                         above (issue #668 iter3). -->
                                                                    <Grid Margin="0,3,0,0">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6">
                                                                            <!-- Icon size driven by CardIconSize / CardFallbackFontSize resources at the top of the page. -->
                                                                            <Grid Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                                  VerticalAlignment="Center">
                                                                                <Image Source="{Binding IconSource}"
                                                                                       Width="{StaticResource CardIconSize}" Height="{StaticResource CardIconSize}"
                                                                                       Stretch="Uniform"
                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                       Visibility="{Binding HasIcon, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                                <fluentIcons:FluentIcon Icon="Apps" IconVariant="Color"
                                                                                                        FontSize="{StaticResource CardAppsIconSize}"
                                                                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                                        Visibility="{Binding IsAppXFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                                            </Grid>
                                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                                          Foreground="{ThemeResource BadgeRecommendedForeground}"/>
                                                                            </Viewbox>
                                                                            <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                                                                     Visibility="{Binding IsInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                                                     ToolTipService.ToolTip="{Binding InstalledStatusText}">
                                                                                <PathIcon Data="{StaticResource DesktopTowerMonitorIconPath}"
                                                                                          Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
                                                                            </Viewbox>
                                                                        </StackPanel>
                                                                        <TextBlock Grid.Column="1"
                                                                                   Text="{Binding Name}"
                                                                                   VerticalAlignment="Center"
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   FontSize="16"
                                                                                   Margin="6,0,0,0"
                                                                                   MaxWidth="{StaticResource CompactNameMaxWidth}"/>
                                                                        <HyperlinkButton Grid.Column="2"
                                                                                         NavigateUri="{Binding WebsiteUrl}"
                                                                                         Padding="0" MinHeight="0" MinWidth="0"
                                                                                         VerticalAlignment="Center"
                                                                                         Margin="6,0,0,0"
                                                                                         Visibility="{Binding WebsiteUrl, Converter={StaticResource NotNullToVisibilityConverter}}"
                                                                                         AutomationProperties.Name="{Binding OpenWebsiteAutomationName}">
                                                                            <Viewbox Width="16" Height="16">
                                                                                <fluentIcons:FluentIcon Icon="Open" IconVariant="Regular"/>
                                                                            </Viewbox>
                                                                        </HyperlinkButton>
                                                                    </Grid>
                                                                </CheckBox>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Expander.Content>
                                            </Expander>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Loading overlay for External Apps -->
                        <Grid Grid.Row="1"
                              Visibility="{x:Bind ViewModel.ExternalAppsViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                              Background="{ThemeResource LayerFillColorDefaultBrush}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                                <ProgressRing IsActive="True" Width="48" Height="48"/>
                                <TextBlock Text="{x:Bind ViewModel.ExternalAppsViewModel.StatusText, Mode=OneWay}"
                                           Style="{StaticResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
